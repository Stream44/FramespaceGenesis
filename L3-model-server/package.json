{
    "name": "@framespace/model-server",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "bun run --watch server.ts",
        "start": "bun run server.ts",
        "test": "bun test"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}
